/** Đồng bộ với backend MAX_TRAINING_IMAGES_PER_STUDENT (mặc định 1000) */
export const MAX_TRAINING_IMAGES_PER_STUDENT = 1000;
export const MIN_TRAINING_IMAGES_TO_RUN = 5;
/** Ngưỡng khuyến nghị để nhận diện ổn định */
export const RECOMMENDED_TRAINING_IMAGES = 50;
/** Khoảng cách giữa các lần chụp tự động (ms) */
export const AUTO_CAPTURE_INTERVAL_MS = 280;
